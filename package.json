{
  "name": "off-the-wall",
  "version": "1.4.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --format=iife --outfile=docs/app.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
